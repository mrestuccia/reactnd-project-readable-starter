import {requestCategories} from './categories'
import {requestPosts, updateVote} from './posts'
import {requestComments, addComment, deleteComment} from './comments'

export {
  requestCategories,
  requestPosts,
  updateVote,
  requestComments,
  deleteComment
}